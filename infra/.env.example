JWT_SECRET=change-me-in-prod
JUDGE_MODEL=ollama/llama3
RATE_LIMIT_PER_MINUTE=120

# Only needed for the optional litellm-proxy service (zero-code tracing).
# Mint one via POST /api/auth/api-keys after registering a user.
OPENEVAL_API_KEY=
# Optional - omit to trace into the API key owner's default project.
OPENEVAL_PROJECT_ID=

# Set these only if you want to evaluate/trace against a paid provider.
# Never commit real keys - this file is a template.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=
